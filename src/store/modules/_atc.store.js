// import { atcService } from '@/services/atc.service';

export const atcStore = ({
    state: {
        atcLabels: null
    },
    getters: {
        
    },
    mutations: {
        
    },
    actions: {
        // async loadAtcLabels(context, { filterBy }) {
        //     const atcLabels = await atcService.query(filterBy);
        //     console.log(atcLabels);
        // }
    }
})